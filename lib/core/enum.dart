enum ListStatus { initial, loading, success, failure }

enum ApprovalStatus { initial, loading, success, failure }

enum ActionStatus { initial, loading, success, failure }

enum Source { detail,approval  }
